# 미니게임 플러그인

가위바위보 선택지마다 plugin variant를 만드는 샘플이다.

## 보여주는 패턴

- factory 함수로 여러 `RuntimePlugin` 생성
- 메뉴 선택값을 plugin id에 반영
- 결과 콜백으로 외부 기록 처리를 위임

## 바꿔 쓰는 방향

간단한 게임, 선택형 기능, 상품 옵션, 의상 선택처럼 같은 구조의 plugin을 여러 개 만들 때 참고한다.
