# Common Assets

Shared assets live here when they can be reused by multiple characters or scene sets.

- `parts/`: reusable character-attached props or decorative parts.
- `scenes/`: reusable scene props, foreground objects, backgrounds, and effects.

Character-specific base images should stay under `src/characters/{characterId}/assets/base`.
